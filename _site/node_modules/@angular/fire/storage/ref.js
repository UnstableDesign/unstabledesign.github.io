import { createUploadTask } from './task';
import { from } from 'rxjs';
export function createStorageRef(ref, schedulers, keepUnstableUntilFirst) {
    return {
        getDownloadURL: function () { return from(ref.getDownloadURL(), schedulers.outsideAngular).pipe(keepUnstableUntilFirst); },
        getMetadata: function () { return from(ref.getMetadata()).pipe(keepUnstableUntilFirst); },
        delete: function () { return from(ref.delete()); },
        child: function (path) { return createStorageRef(ref.child(path), schedulers, keepUnstableUntilFirst); },
        updateMetatdata: function (meta) { return from(ref.updateMetadata(meta)); },
        updateMetadata: function (meta) { return from(ref.updateMetadata(meta)); },
        put: function (data, metadata) {
            var task = ref.put(data, metadata);
            return createUploadTask(task);
        },
        putString: function (data, format, metadata) {
            var task = ref.putString(data, format, metadata);
            return createUploadTask(task);
        }
    };
}
//# sourceMappingURL=ref.js.map